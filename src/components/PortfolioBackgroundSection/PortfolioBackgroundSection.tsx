import { FC } from "react";
import styled from "styled-components";
import PictureProps from "../../types/PictureProps";

const PortfolioBackgroundSection = styled.section``;

export default PortfolioBackgroundSection;
